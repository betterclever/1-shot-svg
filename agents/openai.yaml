interface:
  display_name: "1-Shot SVG"
  short_description: "Generate and locally vectorize web assets"
  default_prompt: "Use $1-shot-svg to create a production-ready website illustration asset through image generation, local VTracer SVG conversion, and PNG fallback."
policy:
  allow_implicit_invocation: true
